# 后端 API 基础路径（VITE_ 前缀一律视为公开信息，禁放任何密钥，web A.2-2）
# 新增 VITE_ 变量必须三处同步：本文件、src/vite-env.d.ts 的 ImportMetaEnv、消费代码
VITE_API_BASE_URL=/api
# 大屏叫号链路令牌（构建期注入的受控演示凭证，订阅级鉴权；默认空=页面显示「未配置大屏令牌」
# 横幅且零出网。VITE_ 公开性由部署面承载——该值随镜像构建注入，不入库真实值）
VITE_BIGSCREEN_TOKEN=
