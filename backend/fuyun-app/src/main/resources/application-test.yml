# test 环境差异：连接细节由 Testcontainers @ServiceConnection 覆盖，本地默认值仅作兜底（backend 宪法 A.2-5）
spring:
  datasource:
    url: ${FUYUN_DATASOURCE_URL:jdbc:postgresql://localhost:5432/fuyun_medical?reWriteBatchedInserts=true}
    username: ${FUYUN_DATASOURCE_USERNAME:fuyun}
    password: ${FUYUN_DATASOURCE_PASSWORD:}
    hikari:
      leak-detection-threshold: 60000   # dev/test 60s 连接泄漏探测（backend 宪法 A.4.2-6，禁 0）
  data:
    redis:
      host: ${FUYUN_REDIS_HOST:localhost}
      port: ${FUYUN_REDIS_PORT:6379}
      password: ${FUYUN_REDIS_PASSWORD:}
  rabbitmq:
    host: ${FUYUN_RABBITMQ_HOST:localhost}
    port: ${FUYUN_RABBITMQ_PORT:5672}
    username: ${FUYUN_RABBITMQ_USERNAME:fuyun}
    password: ${FUYUN_RABBITMQ_PASSWORD:}
    listener:
      simple:
        retry:                     # IT 死信链路快速收敛：重试 3 次亚秒级完成（application.yml 3 次退避姿态不变）
          initial-interval: 100ms
          multiplier: 1.0
management:
  endpoint:
    health:
      show-details: always      # test 排障便利：健康明细全暴露
# M02 患者域字段加密密钥（test 兜底合成值：仅测试上下文启动用，非生产密钥，先记于 CHANGELOG 红线不含真实凭据）
# Task 14 装配后该组属性是 fuyun-app 全部 IT 上下文启动前置；EmpiGovernanceIT 的 @DynamicPropertySource 可继续覆写
fuyun:
  patient:
    crypto:
      data-key: ce714e506dc0737927b2ed726f66a9f24cfdfdfee47c3992c2ec3bd2b66b7cba
      mac-key: 17062cfcde2eff5196246bf128169cce6fa9fdf98096aaabb70711dfd4938f6b
