/**
 * M06 药事域枚举包（A.2-7：统一 enum 类型，携带业务 code 的枚举须实现 code↔enum 双向映射）。
 *
 * <p>TODO(P1): 15 枚举的 code↔enum 映射与 impl 状态字面量（String 形态为 PR-4 brief 冻结口径）随状态机 String→枚举一并改造，计划于 P1 状态机枚举化改造时引入。
 */
package com.fuyun.pharmacy.enums;
