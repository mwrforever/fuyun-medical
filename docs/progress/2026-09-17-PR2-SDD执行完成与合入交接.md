# 任务交接文档 (Session Handoff)

> ⚠ **交接声明（务必先读）**：本文档由 `session-handoff` 技能生成，是新会话续接任务的**唯一事实来源**，内容仅来自当前会话的事实，自包含、不引用其他进度文档或历史会话记录。
> 「第 2 节 未完成」中的全部任务必须在【新会话】执行；生成本文档的会话已交接封存，不再实现任何任务。
> 新会话续接前必须完整读取本文档、以本文档为准，禁止凭空推测进度或重复已完成的工作。
> 代码与文件变更明细不在本文档记录，新会话用 `git diff` / `git log` 自行查看。

## 元信息 (Meta)
- workspace_dir: D:\code\project\fuyun-medical
- saved_at: 2026-09-17T14:20:00+08:00
- 生成方式: update（修正 PR-3 输入为下一份 spec；新增 /code-review 合入前置门标记）
- 版本控制提交号: dev @ 994112c（PR-2 合入点；本地 dev 与 origin/dev 同步）
- 交接状态: 已交接（本会话封存，未完成部分须在新会话执行）
- 任务一句话描述: PR-2（M02 患者主索引 EMPI）SDD 18 任务全流程执行完毕并合入 dev（PR #23 六 checks 全绿）；下一任务为 PR-3 实施计划撰写，本会话新决策与遗留项全部作为其前置项处理。

## 1. 已完成 (Completed) — 只做精简标记
- [x] PR-2 SDD 18 任务: 逐任务实现+spec/quality 双结论审查，6 轮修复闭环，全部合入。
- [x] M02 后端全量: fuyun-patient 从零到全量（V100–V105 11 表+8 事件三段名种子、加密、建档/EMPI/解析/冻结、脱敏/unmask、合并拆分、就诊卡/台账、健康档案、事件发布/缓存失效、Modulith 装配），JaCoCo service.impl LINE=1.00 真实生效。
- [x] EmpiGovernanceIT: CF-3 七场景 Testcontainers 真库全绿；后端全量 verify 24 模块绿。
- [x] openapi 链路首跑: T-R4-3 实证=绿（Springdoc 2.8.17 × openapi-typescript 7.13.0 兼容），api.d.ts 入库+新鲜度空 diff，T-R4-3/T-R4-4 回填删除。
- [x] 前端三页真机: workstation 建档/检索/详情浏览器验证 10 测试点全过（截图证据 `.superpowers/gui-test-screenshots/`）。
- [x] 存量 dev 库承接: 路径 A 完成，V100–V105 六行 success，正式容器 out-of-order env 复验为空。
- [x] 全分支终审: subagent 三次配额耗尽改主控执行，跨切核查全过、挂起 Minor 分诊 0 必修、Spec §7 注记提交。
- [x] CI 合入: commitlint 超长 body 经 filter-branch 折行修复（树零变化）后六 checks 全绿，`gh pr merge --merge --delete-branch` 完成。
- [x] 执行期深层缺陷修复: PatientController 漏装配、V101 快照约束矛盾、split 悬空指针、test profile 缺 crypto 密钥、事件名三段化、AuditActionType 越界、回调单槽位——全部实测修复并先记 CHANGELOG。

## 2. 未完成 (Pending) — 精简且可执行（全部须在新会话执行）

### 2.1 PR-3 实施计划撰写（含前置项处理）
- 目标与当前状态: 按 `superpowers:writing-plans` 技能产出 PR-3（**M13 收费物价与医保基线，CF-4 冻结载体**）实施计划入 `docs/superpowers/plans/`；PR-2 已全部合入，PR-3 尚未立项。
- 已锁定决策/阻塞风险: **本会话新决策——fuyun dev 环境 minio 宿主端口改用 9003/9004 分别映射**（避开本机其他项目 mindsoar-minio 占用的 9000/9001；改 `deploy/docker-compose.yml` minio 端口映射与相关 .env/文档引用，属 PR-3 前置项，本会话未实施）；**新决策——本会话全部新决策/遗留项归入 PR-3 spec 做前置项处理**：TASK.md 现存 D-13（一卡通 close/freeze 并发窗口，M13 接线前收口）/D-14（LOST 找回路径缺定义）/D-15（onsetDate 与 ISO 时刻 500 契约缺口）/D-16（授权三态硬门禁挂接 unmask 归属）/D-17（同证件异名 ID_CARD_CONFLICT 建档路径不可达，产品裁决）/D-18（openapi int64 类型漂移根治）/W-13（PR-2 计划 Task 10 简报勘误 CardUnbindRequest），以及 PR-1b+PR-2 两轮终审 deferred minors 的「模板基类提炼」收口（测试夹具/发布器/缓存失效范式收敛、matchedRules JSON 口径统一、updateRule 词表 @Pattern 校验、快照内容断言补强等，明细 `git log` 与 CHANGELOG 可查）。
- **下一步具体动作**: 新会话读本文档 → **读 PR-3 对应模块 spec `docs/specs/modules/13-billing.md`（M13 需求唯一来源，含其 CF-4 冻结载体定义，见 `docs/specs/90-cross-review.md`）** → `docs/plans/2026-09-14-P1实施计划.md` 仅取 §PR-3 范围行与验收映射（:67 起）→ 读 `TASK.md` 全部 W-/D- 行 → 加载 `superpowers:writing-plans` 撰写 `docs/superpowers/plans/{yyyy-mm-dd}-p1-pr3-m13-billing.md`，计划「前置项」章节显式列入上述 D-13~D-18/W-13/minio 端口/模板基类清单 → 计划获批前不动实现。
- 验证方式: 计划经独立审查（subagent 双轮）后 docs-only PR 合入；SDD 执行前取得用户计划批准（审批门仍有效）；**执行 PR 建 PR 后必过 `/code-review`（code-review@claude-plugins-official）插件审核，审核通过才可合并，否则进入 fix loop 直至 findings 清零**（见 §3 质量门）。

## 3. 环境与全局约定 (Environment & Conventions)
- 运行时/工具版本: JDK 17（系统默认 21，Maven 必须前缀 `JAVA_HOME=/d/code/java/jdk/jdk17`）；Docker 29.7.2；Node/pnpm、Python 3、gh CLI 可用。
- 关键配置: `deploy/.env` 不入库在位（含 FUYUN_PATIENT_CRYPTO_DATA_KEY/MAC_KEY 本地合成 64hex）；真库迁移历史现为 V1~V5/V100~V105/V300~V303/V400~V403/V500~V503；compose 全栈 healthy（backend 为 PR-2 镜像；mindsoar-minio 被本会话 stop 以释放 9000，待 minio 端口决策落地后不再冲突）。
- 构建/启动命令: 起栈 `docker compose -f deploy/docker-compose.yml --env-file deploy/.env up -d`；后端门禁 `JAVA_HOME=/d/code/java/jdk/jdk17 mvn -B -ntp -f backend/pom.xml verify`；前端 `cd web && pnpm lint/format:check/type-check/test/build`；契约重生成 `docker compose exec -T backend curl -fsS http://127.0.0.1:8080/v3/api-docs > web/api-docs.json && cd web && pnpm gen:api`。
- 全局约定: 主控纯编排（实现/审查/修复派 subagent 串行；配额耗尽可改主控执行并记档）；conventional commits 中文 subject 且 **body 每行 ≤100 字符（本地无 commit-msg 钩子，提交前 python 自查）**；先记再改；**实现类 PR 质量门（2026-09-17 用户修订）=全量门禁+真栈+浏览器真机（带 UI 面）→ 建 PR → 交 `/code-review`（code-review@claude-plugins-official）插件审核 → 审核通过才合并，不通过进入 fix loop（修复→复审）直至 findings 清零；免批仅指不等人工批准，插件审核不可跳过**（docs-only PR 免审直合不变）；实现开工前计划审批门有效；gh GraphQL 持续 EOF 时改 REST `gh api repos/.../pulls -X POST -F body=@file`；IAB 真机操作需 `tab.recording.start` 保帧；患者事件三段名 `patient.patient.*` 为冻结契约；patient 后续迁移一律 V500+（W-12）。
- 流程偏差记录: PR #23 合入时未过 `/code-review` 插件审核（当时终审 subagent 三次配额耗尽改主控执行，且该插件门为本次用户指令新立）——自 PR-3 起严格执行新质量门。
