# 任务交接文档 (Session Handoff)

> ⚠ **交接声明（务必先读）**：本文档由 `session-handoff` 技能生成，是新会话续接任务的**唯一事实来源**，内容仅来自当前会话的设计与实现事实，自包含、不引用其他进度文档或历史会话记录。
> 「第 2 节 未完成」中的全部任务必须在【新会话】执行；生成本文档的会话已交接封存，不再实现任何任务。
> 新会话续接前必须完整读取本文档、以本文档为准，禁止凭空推测进度或重复已完成的工作。

## 元信息 (Meta)
- workspace_dir: D:\code\project\fuyun-medical
- saved_at: 2026-09-15T12:30:40+08:00
- 生成方式: rewrite
- 版本控制提交号: dev @ 9c0d569（PR #20 合入点）；工作树干净（仅剩一篇已被 9c0d569 合入的旧交接文档未跟踪残留，可无视）
- 交接状态: 已交接（本会话封存，未完成部分须在新会话执行）
- 任务一句话描述: PR-1b（M20 事件总线治理完整化）的 SDD 执行——计划已合入 dev（PR #20，用户已授权 review+fix loop 完成即合入、不等批准），新会话从 Task 1 起按计划逐任务执行。

## 1. 已完成 (Completed) — 只列结论，让新会话知道哪些不用再做
- [x] **PR-1a（Modulith 事件基础设施）已合入**（PR #16，dev@1d998d5）：Task 1~11 SDD 全流程 + 终审修复波次；迁移落 V500（event_publication）/V501（shedlock）；EventOpsJob 重试/清理挂 ShedLock；ModulithBoundaryTest（含 count≥4 tripwire）；components.puml 依赖图进 CI 产物。
- [x] **D-10/D-11 修宪已合入**（PR #18，dev@984b233）：backend/AGENTS.md B.2-6 补「@ApplicationModuleListener 取 org.springframework.modulith.events 包路径」；C.4 补「单模块构建必须 -am」提示；CHANGELOG 2026-09-15 条目已记。
- [x] **D-12 flaky 修复已合入**（PR #19，dev@556e9d0）：IotAmqpTelemetryConsumerTest 停机排空测试改两处确定性同步（pre-stop 入队计数==2 + post-stop pendingQueueSize==0）；TASK.md D-12 行已删。
- [x] **PR-1b 实施计划已合入**（PR #20，dev@9c0d569）：`docs/superpowers/plans/2026-09-15-p1-pr1b-m20-governance.md`（14 任务、6125 行、88 步）；合入前经独立计划审查（5 Critical + 2 Important 全部实测验证并修复 + scoped re-review 逐条 ADDRESSED + 2 Minor 润色）。
- [x] 前置调研产物（过程文件，未入库）：`.superpowers/pr1b-recon.md`（现状事实 file:line 级核查）、`.superpowers/pr1b-plan-report.md`（写作决策与审查修复记录）。

## 2. 进行中 / 未完成 (Pending) — 重点详述（全部须在新会话执行）

### 2.1 PR-1b 计划的 SDD 执行（Task 1~14，唯一未完成任务）
- 目标: 按 `docs/superpowers/plans/2026-09-15-p1-pr1b-m20-governance.md` 完成 Task 1~14，产出「M20 事件总线治理完整化」实现 PR 并合入 dev。范围：死信处理完整化（查询 API / 重推上限 3 次+留痕 / 关闭）、事件溯源日志（投递/消费/重试全链查询）、主数据分发订阅治理 FU-M20-04、W-4 迁移治理校验脚本、W-5 properties toString 脱敏、W-6 3 Minor、D-8 A.5-9 修宪；新迁移 V502/V503。
- 当前状态: 计划已在 dev（9c0d569）；SDD 零开始——**尚无执行分支、无 Task 1 简报、无 ledger**（上一轮 PR-1a 的 SDD 工作区已按其完成规程删除）。
- 涉及文件/产物: 计划文档（唯一执行依据，改动面见其「文件结构」节）；SDD 工作区由新会话开工时用 `superpowers:subagent-driven-development` 的 `scripts/sdd-workspace PLAN_FILE` 生成（路径 `<repo-root>/.superpowers/sdd/<plan-basename>/`），task-brief 简报按需用 `scripts/task-brief PLAN_FILE N` 生成。
- 已锁定决策（新会话不得推翻）: ①执行方式 = superpowers:subagent-driven-development（每任务全新 subagent + 任务审查双结论 + 修复循环，主控纯编排）；②**用户 merge 授权（2026-09-15）：PR review + fix loop 完成后直接合入，不用等用户批准**——实现 PR 与 docs PR 同此授权；③计划内拍板：重推上限=3 次、D-8 按默认建议修订（先记 CHANGELOG 再改正文）、新迁移自 V502 起、W-5 覆盖 IotProperties/SecurityProperties（该两类外按计划内字段清单说明）；④计划 Global Constraints 与各任务「已锁定决策」全条款。
- 阻塞点/风险（执行期雷区，均为本会话实测拦下）: ①MyBatis-Plus 3.5.17 的 IService/ServiceImpl 在 `com.baomidou.mybatisplus.spring.service[.impl]`（**不是** extension.service）；②`-am` + `-Dtest=` 命令必须带 `-Dsurefire.failIfNoSpecifiedTests=false`（上游模块 surefire 零匹配中止）；③断言错误码用 `isInstanceOfSatisfying(BizException.class, ex -> ex.getErrorCode())`（message 不含 INT-xxxx）；④JaCoCo 包级 LINE=1.00 按模块自身 exec 数据评估（无 report-aggregate），fuyun-app IT 覆盖率不计入 fuyun-integration；⑤W-4 号段守卫合并后，任何新迁移版本必须 >501——TASK.md W-7 计划的「iot V404+」须改 V502+（W-7 行届时同步修订）；⑥计划内三处「二选一/实测为准」步骤按计划内置命令实测后落码。
- **下一步具体动作**: ①`git checkout -b feat/p1-pr1b-m20-governance`（自 dev@9c0d569 创建执行分支）；②加载 superpowers:subagent-driven-development，对计划跑 `scripts/sdd-workspace` 与 `scripts/task-brief ... 1` 生成 Task 1 简报，派实现专员；③逐任务实现+审查至 Task 14，收口推送建 PR。
- 验证方式: 计划内每任务自带验证命令逐任务跑绿；收口 `JAVA_HOME=/d/code/java/jdk/jdk17 mvn -B -ntp -f backend/pom.xml verify` 全绿 + CI 五 checks 全绿 + 真栈探针（重建镜像→compose healthy→真库探针，含 V502/V503 迁移应用与死信重推闭环）+ review findings 清零 → **直接合入（不等批准）**。

## 3. 环境与依赖状态 (Environment)
- 运行时/工具版本: JDK 17（**Maven 命令必须前缀 `JAVA_HOME=/d/code/java/jdk/jdk17`**，系统默认 21）；本地 Maven 仓库在 `/d/code/java/maven/apache-maven-3.9.16/repository`（非 ~/.m2）；Docker daemon 29.7.2 已运行（Testcontainers 与真栈探针需要）；Windows + Git Bash；gh CLI 可用。
- 关键配置: `deploy/.env`（不入库，已在位）；起栈 `docker compose -f deploy/docker-compose.yml --env-file deploy/.env up -d`；查库 `docker exec deploy-postgres-1 psql -U <POSTGRES_USER> -d <POSTGRES_DB>`（键值从 deploy/.env 读）；真库迁移历史现状：V1~V5、V300~V303、V400~V403、V500、V501。
- 安装/构建/启动命令: 后端全量门禁 `JAVA_HOME=/d/code/java/jdk/jdk17 mvn -B -ntp -f backend/pom.xml verify`；镜像重建 `docker build -f backend/Dockerfile -t fuyun/backend:dev backend`；提交前格式化 `mvn -B -ntp -f backend/pom.xml spotless:apply`；编码自查 `python scripts/check-encoding.py`。

## 4. 全局约定 (Locked Conventions)
- **主控纯编排制**: 实现/审核/修复一律派 subagent（串行，禁并行实现），主控只编排、验证关键节点、写编排类小文件；SDD 模式下每任务全新 subagent + 任务审查（spec + quality 双结论），修复轮次按技能 5 轮上限与升级规则；每任务前记录 BASE 提交、审查用 `scripts/review-package` 生成 diff 包。
- **提交规范**: conventional commits、中文 subject、body 每行 ≤100 字符（提交前 python 逐行自查；**type-enum 无 config**，配置类改动用 chore）；先记再改（宪法/规范/机制修订先 CHANGELOG 后正文）；一切变更走 feature 分支 → PR → 五 checks 全绿 → 合入；提交前跑 pre-commit（end-of-file-fixer 等钩子可能改动文件，需二次 add 提交）。
- **宪法强制**: 开工前重读根 `AGENTS.md` 与 `backend/AGENTS.md`（Modulith 条款已定稿于 B.2-6/B.3-2/B.3-3/C.2，D-10 注解包路径与 D-11 -am 提示已入正文）；注释/日志全中文、标识符英文、UTF-8 无 BOM、LF。
- **用户硬性质量门（2026-09-15 更新）**: 实现完成 → e2e/真栈探针（无 UI 面后端变更：重建镜像→compose healthy→真库探针）→ review findings 清零 → **直接合入，不等用户批准**；docs-only PR 免审直合；gh pr checks 用 sleep 循环轮询，勿用 --watch。
- **挂起事项**: TASK.md W-7（非数值遥测入库）用户指示暂不实现，执行 PR-1b 期间禁止顺手实现（其迁移号段届时改 V502+，见 2.1 风险⑤）。
- **内容安全备忘**: 派发 subagent prompt 避免粘贴脱敏示例/医疗数据示例字样，可能触发内容安全误判；agent 失败先查 git log 是否已落盘再决定重派。
