# 任务交接文档 (Session Handoff)

> ⚠ **交接声明（务必先读）**：本文档由 `session-handoff` 技能生成，是新会话续接任务的**唯一事实来源**，内容仅来自当前会话的设计与实现事实，自包含、不引用其他进度文档或历史会话记录。
> 「第 2 节 未完成」中的全部任务必须在【新会话】执行；生成本文档的会话已交接封存，不再实现任何任务。
> 新会话续接前必须完整读取本文档、以本文档为准，禁止凭空推测进度或重复已完成的工作。

## 元信息 (Meta)
- workspace_dir: D:\code\project\fuyun-medical
- saved_at: 2026-09-10T10:30:00+08:00
- 生成方式: rewrite（本次新建）
- 版本控制提交号: dev @ a93179a（PR #6 合并点；工作树有 1 处未提交改动：TASK.md 的 D-3 默认裁决登记行，须随 PR-4 特性分支入库）
- 交接状态: 已交接（本会话封存，未完成部分须在新会话执行）
- 任务一句话描述: 按 `docs/prompt/2026-09-09-loop-P0工程骨架.md`（唯一执行依据）完成 P0 交付：P0 门禁准备 + PR-1~PR-5 依序合入 dev + P6 终验；当前进度 P0~P3 完成，P4 刚起步。

## 1. 已完成 (Completed) — 只列结论，让新会话知道哪些不用再做
- [x] P0 分支与门禁：dev 自 main@a10369e 建立；gh api 配置 dev 保护与 main 五 checks 逐字段一致（`backend / verify`、`frontend / verify`、`images`、`commitlint`、`hygiene` + strict + enforce_admins + 禁 force push/删除）；SDD 台账 `.superpowers/sdd/ledger.md` 建立；冲突扫描完成（仅定稿 §6.6 nginx 路径分歧按 TASK.md W-3 既有裁决归位）；台账经 PR #3 合入 dev（68e777c）
- [x] P1 PR-1 工程骨架（PR #4 合入 dev，ed5e34e）：后端父 POM（Boot 3.5.16 BOM import + spotless 3.4.0 + jacoco 0.8.15 双规则 + surefire/failsafe 分离 + annotationProcessorPaths 四构件）、fuyun-common（异常基座/ProblemDetail 全局渲染含 errorCode+traceId/TraceIdFilter/OperatorContextHolder，已 @Import 装配生效）、fuyun-app（P0 依赖集 + 三 profile yml + flyway 20 目录显式枚举）、20 业务域空模块；SmokeStackIT（Testcontainers 三容器同 compose tag）；backend/Dockerfile 逐模块 COPY 重写；deploy/ 全量（七服务 compose + .env.example 23 键 + initdb/rabbitmq.conf/nginx fuyun.conf）；ci.yml 移除骨架期排除项；web pnpm monorepo 三应用（workstation/portal/bigscreen，element-plus 仅 workstation）+ packages/shared·ui + 根 eslint/prettier/vitest；web/Dockerfile 补 --from=build；compose 六服务两轮起停全 healthy；vite base 三应用子路径适配
- [x] P1 期间 CI 机制修复（已随 PR #4 合入）：images job 安装 buildx（docker driver 不支持 gha 缓存导出）；images job 去 matrix 化改为两显式构建步骤（matrix 展开会使 required check 名 `images` 消失导致分支保护永远 BLOCKED——禁改回 matrix，PR-4 的第三镜像=追加第三个同构构建步骤）
- [x] P2 PR-2 M20 治理构件（PR #5 合入 dev，a019f47）：EventEnvelope 七字段 record + EventEnvelopeCodec + Jackson Long→String 全局定制器；QueueGovernor 统一声明构件（fy.topic/fy.dlx/fy.delay×档位 + q.<消费者>.<事件>，quorum，先登记后订阅 fail-fast）；event_registry V1-V2（integration 号段 V1-V99）；received_event 幂等构件 V3（两层语义）+ dead_letter V4 + 死信监听器；五个 CF-2 事件名（M01 Spec §7 逐字）+ 占位载荷五 record（fuyun-system/api）+ CF-7 StandardTelemetryMessage（common）；V5 七行种子；MessagingGovernanceIT 5 步端到端；宪法回补 v1.3（B.1/C.3 增 handler/ 包、C.5-2 排除增 constants/**）
- [x] P3 PR-3 M01 系统与权限（PR #6 合入 dev，a93179a）：RBAC 五表+关联表 V300 + 字典三表 V301 + 审计表 V302 + 种子 V303（system 号段 V300-V399）；12 枚举（D-6 修宪 enums/，宪法 v1.4）；HMAC-SHA256 令牌（两段式/常量时间比较/六步校验/滑动续期）+ Redis 会话（D-2）+ login/refresh/logout 三端点 + AuthTokenInterceptor（白名单仅 login/refresh）；字典管理 + dict.published 广播（AFTER_COMMIT + Confirm/Returns 告警）+ 消费者（D-7 已落地：幂等 NX 失败回查 received_event）；审计切面（@AuditLog + 双路径 + 口令/令牌/Bearer 头打码 + 列宽收口）+ SensitiveMasker（正则已加数字边界）；practice/check 骨架端点；AuthFlowIT 7 步 + DictBroadcastIT 6 步（含事务回滚负路径）；workstation 登录页/主布局/Axios 单例/store/路由守卫（expiresIn 契约已修为 string）；字典发布已改条件更新防并发双广播
- [x] 测试与门禁现状（dev @ a93179a）：后端 mvn verify 全绿——203 单测 + 21 IT（SmokeStack 3 + MessagingGovernance 5 + AuthFlow 7 + DictBroadcast 6）；JaCoCo 双核心包 LINE=1.00（com.fuyun.system.service.impl 368/368、com.fuyun.integration.service.impl 141/141）；前端六门禁全绿（19 测试）；compose 六服务 healthy 验证通过
- [x] 决策点处理：D-2（令牌=轻量 HMAC+Redis 会话）、D-6（枚举目录=enums/，已修宪）、D-7（幂等 NX 失败回查 DB）——PR-3 前 ask_question 用户未响应，按计划默认/推荐裁决执行并登记 TASK.md（可推翻，推翻改动面已注明）；D-3（iot-simulator=Java + backend/iot-simulator）——P4 前同样按默认裁决，登记行已写入 TASK.md 但**尚未提交**（在 dev 工作树，须随 PR-4 分支入库）
- [x] 过程文档：三份任务简报已归档入库（`.superpowers/sdd/briefs/PR-1/2/3-task-brief.md`）；SDD 台账已随各 PR 更新（台账内 P4/P5 行仍为 pending）

## 2. 进行中 / 未完成 (Pending) — 重点详述（全部须在新会话执行）

### 2.1 P4：PR-4 M14 IoT 通路（当前中断点）
- 目标: 按计划 §1-PR-4 交付 iot 通路全部代码与集成测试并经 PR 合入 dev（IoTDA 真实端到端演示按延后条款处理）
- 当前状态: **PR-4 调研专员（任务简报产出）被取消，简报未产出**；TASK.md 的 D-3 登记行已写入但未提交；除此之外 PR-4 无任何产出
- 涉及文件/产物: `D:\code\project\fuyun-medical\.superpowers\sdd\briefs\PR-4-task-brief.md` [待创建]；TASK.md D-3 行 [已改未提交，在 dev 工作树]；后续按简报产出 B4.1~B4.4 交付物 [待创建]
- 已锁定决策: D-3 = Java + Maven 子模块 `backend/iot-simulator`（默认裁决，可推翻）；**images 第三镜像 = 在既有 images job 追加第三个显式构建步骤（禁改回 matrix——分支保护 required check 名 `images` 会对齐失效）；iot-simulator 路径在 `backend/**` 下，changes 过滤器天然覆盖，无需改过滤器**；宪法 A.5-9/10 Qpid 全约束（独立 iot.amqp.* 前缀与容器工厂、URI 显式 failover 参数 initialReconnectDelay=3000/reconnectDelay=3000/maxReconnectDelay=30000、SmartLifecycle、连接数预算实例×每实例≤32、积压水位+断链时长双指标）；超表顺序 CREATE TABLE→create_hypertable 按天分区→压缩（7 天后）→保留（明细 90 天），分区列入唯一键 (device_id, metric_code, occurred_at)；T-R3-2 实测择一（add_columnstore_policy vs add_compression_policy）非用户决策；AMQP 客户端确认与 RabbitMQ AUTO 确认是两套（代码注释必须写清）
- 阻塞点/风险: 本地 `.env` 无 IOTDA_* 六变量（用户侧准备项未就绪）→ 按计划延后条款执行：交付代码+集成测试（模拟信封注入/本地 broker），`--profile sim` 端到端演示延后并在 PR-4 收口时登记 TASK.md；T-R3-3（断链 10 分钟重连）无 IoTDA 时的可测边界需简报给出结论（本地 AMQP broker 或延后登记）；新依赖须申报（STOMP 需 spring-boot-starter-websocket/messaging，BOM 托管；simulator 的 MQTT 客户端选型须给版本来源）
- **下一步具体动作**: ① `git switch -c feat/pr4-m14-iot`（dev @ a93179a，携带 TASK.md 未提交改动），首提交 `chore(task): 登记 D-3 默认裁决（iot-simulator Java 子模块）`；② 派调研专员产出 PR-4 简报（输入：计划 §1-PR-4、docs/specs/modules/14-iot.md 全文、backend 宪法 A.4.1-5/A.5-9/10/B.3、既有 fuyun-integration 幂等与 QueueGovernor、fuyun-common StandardTelemetryMessage、deploy compose 的 iot-simulator 占位、backend/Dockerfile COPY 清单、ci.yml images job；输出须含：T-R3-2 实测方案与择一结论登记方式、iot 号段建议（建议 V400-V499，核对避让表）、B4.1~B4.4 逐文件规格（B4.1 iot 迁移+超表+策略；B4.2 Qpid 工厂+SmartLifecycle 消费者+四路同构解析+攒批写 500-5000/批+客户端确认+iot_consume_error_log+双指标；B4.3 /ws/iot STOMP 端点+遥测摘要主题+fy.topic 扇出+POST /ingest/iotda-fallback 独立鉴权；B4.4 simulator 子模块+images 第三步骤+T-R3-3 处置）、模拟信封注入 IT 方案、表外依赖申报清单、IOTDA 延后登记口径）——沿用 `docs/prompt/2026-09-09-loop-P0工程骨架.md` 第四章派遣六要素与前三份简报的结构风格
- 验证方式: 批次循环同 P1~P3（实现专员 TDD 顺序执行 → 批次审核双结论 → 终审 → PR → 五 checks 绿 → /code-review → 合入）；B4.1 出口含迁移重放 + T-R3-2 结论；B4.4 出口含镜像构建成功
- 建议会话节奏: 每个批次一个实现专员派遣（严格顺序、禁并行），简报一次产出覆盖 B4.1~B4.4

### 2.2 P5：PR-5 收口
- 目标: bigscreen 最小遥测页（订阅 /ws/iot 遥测摘要主题）+ workstation 首页骨架 + TASK.md W-3 销项 + T-R3-2/T-R3-3 实测结论回填 + 计划文档勾选 + DoD 预检
- 当前状态: 未开始（前置 PR-4 未完成）
- 涉及文件/产物: `web/apps/bigscreen/`（需新增 @stomp/stompjs 7.3.0 依赖——表外申报）、TASK.md、docs/plans/2026-09-08-P0实施计划.md [修改]
- 已锁定决策: 浏览器 E2E 维持排除（链路级口径）；PR 依序合入 dev；最终停留 dev（dev→main 不在 P0 范围）
- 阻塞点/风险: STOMP 封装须守 web 宪法 B.3-3（每 app 单例 Client/库内建重连/卸载 unsubscribe/token 经 beforeConnect）
- **下一步具体动作**: PR-4 合入后建 `feat/pr5-p0-closeout`，派调研专员出 PR-5 简报（输入：计划 §1-PR-5、web 宪法、TASK.md 现状）
- 验证方式: 前端六门禁绿 + TASK.md/计划文档回填完整 + DoD 预检清单

### 2.3 PR-2 的 PR 级 /code-review 缺口（P6 终验前须处置）
- 目标: 满足 DoD「五个 PR 均经 /code-review 审核通过后合入 dev」
- 当前状态: PR #4 与 PR #6 在 PR 时点各派过一次独立 /code-review 专员（findings 均已修复）；**PR #5（PR-2）合入前只做了 B2.1/B2.2/B2.3 三轮批次审核 + 全分支终审（四轮独立审查），未单独派 PR 时点 /code-review 专员**——流程偏差
- 涉及文件/产物: 无代码产物；结论登记到终验报告
- 已锁定决策: 无
- 阻塞点/风险: P6 终验 DoD 第 3 条逐项核对时会暴露该缺口
- **下一步具体动作**: P6 终验期对已合入 diff `git diff ed5e34e..a019f47`（PR-2 全量）补跑一次独立 /code-review 专员（模拟 /code-review 插件、置信 ≥80 findings），有 findings 则评估：可随 P4/P5 批次修复的登记处理，纯历史问题登记 TASK.md；结果写入终验报告
- 验证方式: 终验报告含该次 review 结论与处置记录

### 2.4 P6：终验
- 目标: DoD 逐项附证据 → 终验报告 `docs/prompt/2026-09-09-P0终验报告.md` 经 docs PR 合入 dev → 台账收尾 → 本地停留 dev
- 当前状态: 未开始
- 涉及文件/产物: docs/prompt/2026-09-09-P0终验报告.md [待创建]；.superpowers/sdd/ledger.md [更新]
- 已锁定决策: DoD 七条以执行文档 §5 为准（compose 七服务 healthy + sim 链路演示或延后登记；后端门禁含 JaCoCo 双阈值核心包 1.00；前端五门禁；五 PR 经 /code-review 合入 dev；main 与 dev 保护均含五 checks（gh api 输出落终验报告）；CF-1/CF-2/CF-7 冻结登记 event_registry；TASK.md W-3 销项、T-R3-2/3 回填；台账完整、终验报告合入 dev、停留 dev）
- 阻塞点/风险: `--profile sim` 端到端演示依赖 IOTDA_*，缺失则按计划延后登记（不阻塞）；T-R3-2/3 回填依赖 PR-4 实际完成质量
- **下一步具体动作**: PR-5 合入后按 DoD 逐项跑命令留证据（verification-before-completion：先跑后声明），撰写终验报告，走 docs PR 合入 dev
- 验证方式: DoD 全勾且每项附命令输出/gh api 输出；当前分支=dev

## 3. 环境与依赖状态 (Environment)
- 运行时/工具版本: Windows + Git Bash；JDK 17 位于 `D:\code\java\jdk\jdk17`（**系统默认 JDK 是 21，Maven 命令必须前缀 `JAVA_HOME=/d/code/java/jdk/jdk17`**）；Maven 3.9.16；Docker 29.7.2（**Docker Hub 直连不通，用 `docker.m.daocloud.io` 拉取后重打 tag**）；Node v24.19.0；pnpm 12.3.4（corepack 已启用）；gh 已登录 mwrforever（repo+workflow scopes，分支保护管理可用）
- 关键配置: 本地 `deploy/.env`（不入库）已填测试口令 + `MINIO_PORT=19000`（9000 被他人容器占用，禁动）+ `FUYUN_SECURITY_TOKEN_HMAC_SECRET`（64 位随机值）；`.env` 无 IOTDA_*（延后条款生效中）；git 用户 mwr / 18229923842@163.com
- 安装/构建/后端全量门禁: `cd backend && JAVA_HOME=/d/code/java/jdk/jdk17 mvn -B -ntp verify`（Testcontainers 需 Docker，本机全绿基线 203 单测+21 IT）；前端门禁 `cd web && pnpm lint && pnpm format:check && pnpm type-check && pnpm test && pnpm build && pnpm audit --audit-level high`；本地起栈 `docker compose -f deploy/docker-compose.yml --env-file deploy/.env up -d`
- 流程硬约束: main/dev 均有分支保护（PR 必走、直推被拒）；台账/登记类小改动随下一个特性分支提交，禁止直推 dev

## 4. 全局约定 (Locked Conventions)
- **执行依据层级**: `docs/prompt/2026-09-09-loop-P0工程骨架.md`（loop，唯一执行依据）→ `docs/plans/2026-09-08-P0实施计划.md`（PLAN-P0-01，交付物唯一 spec）→ 三级宪法（根/backend/web AGENTS.md）→ 技术栈定稿 `docs/language/2026-09-07-技术栈选型.md`（版本唯一权威）
- **主控编排制**: 调研/实现/审核一律派 subagent，主控只编排；实现专员严禁并行；每批次=调研简报→实现（TDD RED→GREEN）→批次审核（SPEC+QUALITY 双结论）→修复循环（≤5 轮）→全批次毕终审→PR→CI 五 checks→/code-review（置信≥80 findings 须核实修复）→合入 dev→删分支→台账登记
- **提交规范**: conventional commits、中文 subject、body 每行 ≤100 字符（commitlint 门禁；本地未装 husky，提交前自查）；先记再改（CHANGELOG.md）
- **提交消息折行法**: 超长时用「锚点切片」在语义边界折行（去换行后与原文逐字符一致），reword 用 GIT_SEQUENCE_EDITOR+edit 非交互 rebase
- **宪法现行版本要点**（backend v1.4）: 枚举包目录 `enums/`（D-6）；`handler/` 包=MyBatis TypeHandler；jacoco 排除含 `constants/**`；核心包 PACKAGE LINE 1.00 已生效包=com.fuyun.system.service.impl、com.fuyun.integration.service.impl（PR-4 的攒批/解析实现若落 service.impl 将自动纳入 1.00 门禁）；MP 3.5.17 的 ServiceImpl/IService 在 `com.baomidou.mybatisplus.spring.service.*` 包（3.5.17 实证）；@EnumValue 落字段（仅 FIELD 目标）+@JsonValue 落 getter
- **Flyway 号段**: integration=V1-V99（已用 V1-V5）、system=V300-V399（已用 V300-V303）、iot 建议 V400-V499（PR-4 简报确认）；迁移禁改已应用版本
- **幂等消费范式**（含 D-7）: Redis SET NX 前置（失败回查 received_event 唯一索引，命中才跳过）+ 唯一索引兜底 + 失败释放；Redis 故障降级放行
- **序列化契约**: Jackson 全局 Long/long→String（含原生 long，已实证）；前端对应字段一律 string 承载（expiresIn 教训：原生 long 也序列化为字符串）
- **CI 五 checks 名精确对齐**: `backend / verify`、`frontend / verify`、`images`、`commitlint`、`hygiene`；images job 禁用 matrix（check 名会展开）；frontend job 步骤为 pnpm install --frozen-lockfile → lint → format:check → type-check → test → build → audit --audit-level high
- **用户决策状态**: D-2/D-3/D-6/D-7 均按计划默认裁决执行（用户两次 ask_question 未响应），TASK.md 各行已注明「可推翻」+推翻改动面；新会话若获用户推翻裁决，按登记的改动面回收
