# N4 修复派遣简报（实现专员必读）

> 你的派遣提示会给出修复项 ID；修复方案以两份定稿清单为唯一依据：
> - `D:\code\project\fuyun-medical\docs\progress\2026-09-23-安全扫描问题清单.md`
> - `D:\code\project\fuyun-medical\docs\progress\2026-09-23-性能与算法优化清单.md`
> （清单在主工作区，只读；所有代码改动只发生在 worktree。）

## 0. 铁律

1. **只在 worktree 工作**：`D:\code\project\fuyun-medical\.worktrees\fix-scan-20260923`（分支 `fix/perf-security-scan-20260923`）。主工作区 `D:\code\project\fuyun-medical` 绝对只读。
2. **只修清单内本项**：禁顺手修相邻问题；发现清单外问题只在你最终回复中报告（主控登记），不动代码。
3. **行为保持**：不改 API 契约/DB 既有 schema/配置格式/公共组件契约；增量迁移只新增文件，禁改既有迁移（A.4.1-3 红线）。
4. **规范**：先读 `backend/AGENTS.md`（或 `web/AGENTS.md`）与对应模块 `docs/specs/modules/*.md` 确认语义；注释/日志全中文（业务意图与为什么）；UTF-8 无 BOM、LF；移除未用 import；测试与实现同提交。
5. **一问题一提交**：Conventional Commits（如 `perf(nursing): ...` / `fix(patient): ...`），header ≤72 字符，body 每行 ≤100 字符，body 必含：修复项 ID / 根因 / 修复方式 / 验证方式。只 `git add` 本项涉及文件（禁 -f）。提交前 `git status` 确认无越界文件。
6. **方案受阻即停**：若清单方案与 specs 语义冲突或需破坏性变更，停止并报告（勿自行改方案——主控回 N2 修订）。

## 1. 局部门禁（提交前必须绿）

- 后端项：worktree 根执行 `mvn -B test -pl <受影响模块> -am`（Docker 已就绪，Testcontainers IT 可跑）。
- 前端项：worktree `web/` 执行 `pnpm lint && pnpm type-check && pnpm test`。
- 门禁输出摘要附在最终回复（勿全量粘贴）。

## 2. ALGO 项协议（行为保持对照）

先写对照测试（旧实现取值路径 vs 新实现取值路径结果一致，含边界与 tie 场景）→ RED 或直接对照实现 → 优化实现 → GREEN + 对照复验通过。提交信息与测试命名体现复杂度改善说明（时间/空间量级）。

## 3. 完成标准

代码+测试落盘、局部门禁绿、单次提交完成、最终回复精简：改动文件清单 + 提交 hash + 门禁摘要 + 清单外发现（如有）。
