# 全仓性能安全扫描 · e2e 验收记录（N5）

> 执行时间：2026-09-24；验收对象：fix 分支 `fix/perf-security-scan-20260923`（6 修复提交 7aa1c90/e15598a/7f4e611/3127b8c/ffd5711/11b696d）。
> 环境：隔离 compose 项目 `fuyun-e2e-fixscan`（自 worktree `.worktrees/fix-scan-20260923` 编排，后端镜像 `fuyun/backend:fixscan-e2e` 自 worktree 源码多阶段构建），端口重映射 8081/5434/6381/5673/15673/9005/9006，与主工作区运行栈（deploy-* 项目）及无关项目（mindsoar-*）零冲突、零扰动。入口 `http://localhost:8081`（nginx /healthz ok）。
> 全新空库：Flyway 全链 V1→V900 一次通过（`flyway_schema_history` 808/900 两行 success=t），即两条新增量迁移在干净库上的落位验证。

## 一、覆盖矩阵（修复项 → 场景，100% 覆盖无孤项）

| 修复项 | e2e 场景 | 结果 | 证据摘要 |
| --- | --- | --- | --- |
| PERF-02 | 迁移落位 + 患者维度体征查询：`GET /api/v1/nursing/vital-signs?patientId=2103099413635928065` | ✅ 通过 | 干净库 Flyway V808 success=t；`pg_indexes` 实测 `idx_vital_sign_patient_time ON nursing.vital_sign_record USING btree (patient_id, measured_at)`；API 200 返回 3 行且按 measured_at 升序 `[9100001@01:00Z 36.5, 9100002@05:00Z 37.2, 9100003@05:00Z 38.9]` |
| PERF-03 | 迁移落位 + 姓名子串检索命中集：API 建档 张伟/张伟明/李娜 后 `GET /patients/search?keyword=伟` | ✅ 通过 | 干净库 Flyway V900 success=t + `pg_extension` pg_trgm 在位；`pg_indexes` 实测 `idx_patient_name_trgm USING gin (name gin_trgm_ops)`；检索 200 total=2 命中 张伟(2103099412826427394)/张伟明(2103099413161971713)，李娜(2103099413405241346) 排除，姓名按 MASK_NAME 掩码出网 |
| ALGO-01 | 行为保持对照（真栈点查 vs 升序末位语义）：入区登记（POST /nursing/ward-patients 200）+ SQL 种子三条体征（含 05:00Z 同刻 tie 对 id 9100002/9100003）后 `GET /nursing/pda/patient-summary?identifier=I2026092400001` | ✅ 通过 | 200；latestVitals = `{id:9100003, measuredAt:2026-09-24T05:00:00Z, temperature:38.9, tempSite:AXILLARY}`——与「升序取末位」语义一致且同刻 tie 确定性取 id 较大行；ward 上下文 W01/01 正确装配 |
| PERF-01 | 退费全链 API 级集成：手工计费（qty10×10000 分）→ 预结算 → 现金结算 100000 分 SETTLED → 退费#1 qty8（80000 分>L1 免审阈值）→ 审批/执行 → 退费#2 qty2 → 执行 → 超退负例 | ✅ 通过 | 计费 201（feeId 2103099758416105473）；预结算 totalAmount=100000；结算 200 SETTLED（id 2103099759120748546）；退费#1 apply 201（PENDING_APPROVAL）→ admin 自审 403「退费不得自审」（双人守卫保持）→ e2e_nurse 审批 204 → admin 执行 204 → 费用行判态 **PART_REFUND**（8/10）；退费#2 qty2=20000 分≤50000 同日落 L0 免审直退（审批端 409 状态拒绝=预期）→ 执行 204 → 费用行 **FULL_REFUND**（8+2=10/10）；超退负例 apply qty1 → **409 拒绝**（结算单已 REFUNDED 终态守卫）；金额与判态全程由新聚合下推路径计算，与旧实现口径一致 |
| SEC-01 | 脱敏规则维护门禁对照：无角色用户 e2e_nurse（sys_user 910001，零 sys_user_role 绑定）vs admin 各调 `PUT /api/v1/patient/privacy-mask-rules/MASK_NAME` | ✅ 通过 | nurse → **403 PAT-1024**「脱敏规则维护仅限系统管理员」（提权链堵断，服务零触达）；admin → **200** 返回规则体 `{ruleCode:MASK_NAME, maskPattern:KEEP_FIRST, exemptRoles:[ADMIN], enabled:true}` |
| SEC-02 | 候诊列表身份比对对照：admin（会话 userId=1）调 `GET /api/v1/outpatient/doctor/patient-queue?deptCode=D01&doctorId=<1|910001>` | ✅ 通过 | doctorId=1（与会话一致）→ **200**（空队列合法返回）；doctorId=910001（他人）→ **403 OP-1020**「候诊列表仅允许查询本人队列」 |

## 二、场景执行留痕

- 双账号：admin（V303 种子，userId=1）与 e2e_nurse（e2e 夹具注入 userId=910001，bcrypt 同种子哈希、零角色绑定，`SELECT count(*) FROM system.sys_user_role … =0` 实证）。
- 夹具先例：患者主档经 API 正常建档（EMPI NO_MATCH 新建），入区经 API 正常流转；仅体征时间点（业务时间服务器生成不可注入）与计费目录种子经 SQL 直插——与仓内 NursingVitalSignFlowIT/OutpatientFullFlowIT 既定夹具口径一致（上游主数据直插、受验路径全 API 驱动）。
- 关键响应原文存档（traceId 抽样）：SEC-01 403 `traceId=1d7407ee-58ba-44c7-8e97-f0a42d87c5cb`；SEC-02 403 `traceId=faca1f46-ede8-4166-9955-da3ea1428c08`；超退 409 `traceId=5e0f5553-251d-464e-b9a2-e0a9eb3eee58`。
- 回退循环：0 次（全部场景一次通过，无 N4 回退）。

## 三、结论

覆盖矩阵 6/6（100%），全场景通过且各含正/负路径断言；业务零破坏（既有守卫——双人审批、免审分级、结算终态、脱敏掩码——全部行为保持）。N5 出口门禁满足。
