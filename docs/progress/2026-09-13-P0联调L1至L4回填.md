# 任务交接文档 (Session Handoff)

> ⚠ **交接声明（务必先读）**：本文档由 `session-handoff` 技能生成，是新会话续接任务的**唯一事实来源**，内容仅来自当前会话的设计与实现事实，自包含、不引用其他进度文档或历史会话记录。
> 「第 2 节 未完成」中的全部任务必须在【新会话】执行；生成本文档的会话已交接封存，不再实现任何任务。
> 新会话续接前必须完整读取本文档、以本文档为准，禁止凭空推测进度或重复已完成的工作。

## 元信息 (Meta)
- workspace_dir: D:\code\project\fuyun-medical
- saved_at: 2026-09-13T23:41:44+08:00
- 生成方式: update（更新本会话的文档：L-3 处置已获用户批准，选定选项 B）
- 版本控制提交号: dev @ 9b1d22f（PR #11 合入点，工作树干净）
- 交接状态: 已交接（本会话封存，未完成部分须在新会话执行）
- 任务一句话描述: P0 交付闭环后执行 IOTDA 联调（TASK.md L-1~L-4）：已打通 MQTT 上行与 AMQP 认证（两轮缺陷修复 PR #10/#11），毒丸隔离留痕实证 569 帧，真实报文结构已取证——L-3 报文映射已获用户批准（选项 B），新会话直接实施。

## 1. 已完成 (Completed) — 只列结论，让新会话知道哪些不用再做
- [x] P0 交付主循环全部退出：五 PR（#4 骨架/#5 治理构件/#6 系统权限/#7 IoT 通路/#8 收口）+ 终验报告 docs PR #9 均合入 dev；DoD 七条逐项附证据；本地停留 dev（终验报告 `docs/prompt/2026-09-09-P0终验报告.md`）
- [x] PR #7（M14 IoT 通路）与 PR #8（P0 收口）的完整批次循环、终审、/code-review 修复均在本会话前半段完成并合入（详见 git log 与台账 `.superpowers/sdd/ledger.md` 全量批次明细）
- [x] PR-2 /code-review 补跑通过（3 Minor 登记 TASK.md W-6）；CI 门禁缺陷修复（骨架期 pom/webpkg 守卫退役，随 PR #8）
- [x] 联调配置：`deploy/.env` 八项已配齐——用户填四凭证（IOTDA_AMQP_ACCESS_KEY/SECRET、IOTDA_DEVICE_ID/SECRET）+ 主控补四键（IOTDA_AMQP_ENDPOINT=amqps://2cc851655e.iotda-app.cn-south-4.myhuaweicloud.com:5671、IOTDA_MQTT_HOST=ssl://2cc851655e.iotda-device.cn-south-4.myhuaweicloud.com:8883、FUYUN_IOT_AMQP_QUEUES=fuyun-telemetry、FUYUN_IOT_AMQP_ENABLED=true）；文件确认被 .gitignore 忽略不入库
- [x] PR #10（已合入 dev@ed472ae）：禁用 JMS 健康指标（`management.health.jms.enabled=false`）——JmsHealthIndicator 无凭证探测强制 SASL PLAIN 的 IoTDA 恒 DOWN 致容器 unhealthy、simulator 依赖失败；修复后 backend healthy
- [x] PR #11（已合入 dev@9b1d22f）：AMQP 凭证改官方格式——官方《AMQP客户端接入说明》（iot_01_00100_2）证实 PR-4 预判错误：username=三段竖线拼接 `accessKey={key}|timestamp={13位毫秒}|instanceId={可选空段}`、password=accessCode 原值无拼接、amqps URI 补 `amqp.vhost=default&amqp.idleTimeout=8000&amqp.saslMechanisms=PLAIN`；修复后认证通过持续收帧
- [x] 链路取证：simulator→IoTDA MQTT/TLS 上行通（`MQTT 已连接：deviceId=6aa570ac155456566827c784_fuyun-demo-001`，5 秒周期）；backend AMQP 消费通（无 SASL 错误）；supervisor 退避重建在真实 IoTDA 上实证（12s→24s→30s 封顶）
- [x] 毒丸隔离实证：`iot.iot_consume_error_log` 已留痕 569 帧（stage=PARSE、确认抛弃、队列不堆积）——真实报文与 CF-7 契约不匹配即 L-3，毒丸机制按设计兜住
- [x] 真实报文结构取证（L-3 映射规格依据，取自 raw_payload）：`{"resource":"device.property","event":"report","event_time_ms":"2026-09-12T17:30:41.632Z","notify_data":{"header":{"device_id":"6aa570ac155456566827c784_fuyun-demo-001","node_id":"fuyun-demo-001","product_id":"6aa570ac155456566827c784"},"body":{"services":[{"service_id":"Monitor","properties":{"heartRate":78,"spo2":100},"event_time":"20260912T173041Z"}]}}}`
- [x] 镜像与栈：`fuyun/backend:dev`、`fuyun/iot-simulator:dev` 已按最新代码重建；`--profile sim` 起栈验证通过（六服务 healthy + simulator Up；backend healthcheck 探 actuator/health，iot-simulator depends_on backend healthy）

## 2. 进行中 / 未完成 (Pending) — 重点详述（全部须在新会话执行）

### 2.1 L-3 报文映射（当前中断点，已获用户批准：选项 B）
- 目标: `TelemetryFrameParser` 新增「IoTDA AMQP 推送报文」第三形态，真实报文展开为 N 条 CF-7 遥测，下游管道零改动
- 当前状态: **用户已于 2026-09-13 批准选项 B（代码映射），无需再次请示，新会话可直接开工**；真实报文结构已取证（§1 末条）
- 涉及文件/产物: `backend/fuyun-iot/src/main/java/com/fuyun/iot/internal/TelemetryFrameParser.java` [修改]、`TelemetryFrameParserTest` [修改]、毒丸/管道 IT [修改]；走 fix PR 流程（参考 PR #10/#11 模式：fix 分支→CHANGELOG 先记再改→TDD→PR→五 checks→合入）
- 已锁定决策（新会话不得推翻）: **方案=选项 B（已批准）**；判别=`resource=="device.property"`；deviceId←`notify_data.header.device_id`；occurredAt←`event_time_ms`（ISO-8601 解析 Instant）；`body.services[].properties` 键值遍历——每键一条 CF-7（metricCode=属性名如 heartRate/spo2，P1 建字典再规范化）；非该形态继续走既有 CF-7/状态帧判别；下游（攒批/落库/推送）零改动
- 阻塞点/风险: 无（批准已取得）；注意 value 类型——真实报文 properties 值为数值（78/100），CF-7 的 value 为字符串承载（StandardTelemetryMessage 契约），映射时需 String.valueOf 并保持 quality=GOOD
- **下一步具体动作**: 派实现专员按上述锁定决策 TDD 开发（自 dev 建 `fix/iotda-payload-mapping` 分支；CHANGELOG 先记再改登记 L-3 冻结结论；`mvn -B -ntp verify` 全绿；提交 body 每行 ≤100 字符 python len 自查）；完成后 PR 合入 → 重建 backend 镜像（§3 命令）→ 重启容器验证
- 验证方式: backend 日志毒丸帧归零；`iot.iot_telemetry` 行数按 5 秒/帧×属性数增长（patient_id/visit_id 为 NULL——无绑定数据，正常）；`SELECT * FROM iot.iot_telemetry ORDER BY occurred_at DESC LIMIT 5` 可见 heartRate/spo2 行

### 2.2 L-1~L-4 回填收口（依赖 2.1 完成）
- 目标: 全链路（IoTDA→AMQP→TimescaleDB→WebSocket）演示记录与 TASK.md 四条延后登记回填
- 当前状态: 未开始（等 L-3）；STOMP 推送链路已由 IotTelemetryPipelineIT 保障，真实栈验证待遥测落库后进行
- 涉及文件/产物: `TASK.md` L-1~L-4 行 [回填删除]、`CHANGELOG.md` [登记]、台账 [更新]
- 已锁定决策: L-3 完成后 L-1/L-3 一并回填；L-2（真实 IoTDA 端点 10 分钟断链演示）需保持栈运行并断网/凭证过期实验 10 分钟窗口（supervisor 真实重建已有 12s→24s→30s 退避证据，10 分钟窗口实验为回填口径）；L-4（真实积压水位指标=IoTDA 侧最旧未消费消息年龄）取 IoTDA 控制台/指标口径回填
- 阻塞点/风险: simulator 只发 properties 上报（无设备状态帧），设备状态链路（iot_device.status 更新+STOMP 设备状态主题）在真实栈不可验证——回填 L-1 时注明该口径（管道已由 IT 覆盖）
- **下一步具体动作**: L-3 合入后：`docker compose -f deploy/docker-compose.yml --env-file deploy/.env --profile sim up -d --force-recreate backend` → 观察 `docker logs deploy-backend-1` 毒丸归零与攒批落库日志 → 查 iot_telemetry → bigscreen 页（nginx :80/bigscreen/，登录 workstation 取 admin 令牌填入）订阅 `/topic/iot/telemetry/` 验证推送 → 逐条回填 TASK.md L-1/L-2/L-3/L-4 与 CHANGELOG
- 验证方式: TASK.md 延后事项区 L-1~L-4 删除、CHANGELOG 有回填条目、全链路证据落 CHANGELOG

### 2.3 可选收尾（非阻塞）
- 目标: 联调环境完善项
- 当前状态: `FUYUN_IOT_FALLBACK_TOKEN` 未设（compose warning 可忽略；兜底通道 fail-closed 安全默认，仅配置 IoTDA 联动规则 HTTP 推送时需要）
- 涉及文件/产物: `deploy/.env` [用户侧可选补填]
- 已锁定决策: 无
- 阻塞点/风险: 无
- **下一步具体动作**: 若用户想验证兜底端点，让用户自拟随机串填入后重启 backend，以 `curl -H "X-Iot-Fallback-Token: <值>" -d '<CF-7 JSON>' localhost/ingest/iotda-fallback` 验证（需先确认 nginx /ingest/ 路由）
- 验证方式: 202 响应且 iot_telemetry 增行

## 3. 环境与依赖状态 (Environment)
- 运行时/工具版本: JDK 17（Maven 命令必须前缀 `JAVA_HOME=/d/code/java/jdk/jdk17`，系统默认 21）；Docker daemon 29.7.2（**Docker Desktop 在 `C:\Users\Lenovo\AppData\Local\Programs\DockerDesktop\Docker Desktop.exe`，不在 C:\Program Files；daemon 未运行时先启动它**）；Node 24 / pnpm 12.3.4；Windows + Git Bash
- 关键配置: `deploy/.env`（不入库，.gitignore 已覆盖）——IoTDA 八项已配齐（§1）；`FUYUN_SECURITY_TOKEN_HMAC_SECRET`、`MINIO_PORT=19000` 等既有测试值在位
- 安装/构建/启动命令: 重建镜像 `docker build -f backend/Dockerfile -t fuyun/backend:dev backend` 与 `docker build -f backend/iot-simulator/Dockerfile -t fuyun/iot-simulator:dev backend`；起栈 `docker compose -f deploy/docker-compose.yml --env-file deploy/.env --profile sim up -d`；后端门禁 `cd backend && JAVA_HOME=/d/code/java/jdk/jdk17 mvn -B -ntp verify`；前端 `cd web && pnpm lint && pnpm format:check && pnpm type-check && pnpm test && pnpm build`；查库 `docker exec deploy-postgres-1 psql -U <POSTGRES_USER> -d <POSTGRES_DB>`（键值从 .env 读）
- 流程硬约束: main/dev 分支保护（提交一律 PR：feature 分支 → `gh pr create` → 五 checks 绿 → `gh pr merge --merge --delete-branch`）；**commitlint body 每行 ≤100 字符（本会话两次踩坑，提交前必须 python len 自查）**；gh pr checks --watch 易被网络中断，用轮询循环替代

## 4. 全局约定 (Locked Conventions)
- **主控纯编排制**: 调研/实现/审核/修复一律派 subagent（general-purpose），主控只编排、验证关键节点、写编排类小文件（台账/交接文档）；实现专员严禁并行；批次循环=实现（TDD）→审核双结论→修复循环→复审
- **提交规范**: conventional commits、**中文 subject**、body 每行 ≤100 字符、先记再改（CHANGELOG.md）
- **已落码的联调结论（不得回退）**: IoTDA AMQP 凭证官方格式（§1 PR #11）；STOMP 鉴权=CONNECT 帧级 ChannelInterceptor（非 HTTP 握手层）；`management.health.jms.enabled=false`；stompjs 7.3.0 无自动重订阅（客户端 onConnect 重订阅）；Qpid failover 须 `failover.` 前缀+maxReconnectAttempts=3+supervisor 移交；TimescaleDB 压缩策略=CALL add_columnstore_policy
- **执行依据层级**: loop 文档 `docs/prompt/2026-09-09-loop-P0工程骨架.md`（P0 已退出）→ `docs/plans/00-implementation-order.md`（后续 P1 阶段入口）→ 三级宪法 → 技术栈定稿
- **用户决策状态**: **L-3 已批准选项 B（2026-09-13 update 确认），新会话直接开工**；TASK.md 既有默认裁决（D-2/D-3/D-6/D-7）与待裁决（D-8）维持原状；后续新实现事项仍须用户批准（审批门工作流）

## 5. 续接结果（2026-09-14 续接会话回填，§2 任务全部闭环）

- **交接状态更新**: 已完成——§2.1/§2.2 全部执行完毕，§2.3 维持用户侧可选项
- **§2.1 L-3 报文映射**: PR #12 已合入（dev@0584e1e，五提交 e0a3c38..eb623f5，五 checks 全绿）；锁定决策全项落地（判别/deviceId/occurredAt/properties 遍历/下游零改动），另落地 TASK.md L-3 行自带义务 `ConsumePayloadMasker` 毒丸留痕脱敏（白名单提取+正则兜底）；审核 SPEC 一项以路径 b 收口——quality 口径细化声明入 CHANGELOG 2026-09-13 条目，**待用户追认**（登记 TASK.md D-9，改判恒 GOOD 改动面=解析器单行+断言翻转）
- **§2.2 L-1~L-4 回填**: 全链路真栈证据齐备（细节见 CHANGELOG 2026-09-14 条目）——新毒丸归零、遥测每 5 秒落 heartRate/spo2 两行、绑定夹具（iot_binding id=900001，dev 测试数据）后 patient/visit 富化、STOMP 订阅 `/topic/iot/telemetry/1` 收到摘要帧；L-2 断链 10 分 26 秒后 4.5 秒新时间戳凭证重建、积压 252 条一次追平零丢失；L-4 水位应用侧代理口径 10 分 35 秒；TASK.md 延后事项节已删除、台账已补 PR #12 行
- **遗留给用户**: ①D-9 quality 口径追认（二选一，改判成本极低）；②§2.3 FUYUN_IOT_FALLBACK_TOKEN 仍为用户侧可选补填项；③iot_binding 夹具行 id=900001 为联调演示数据，P1 绑定管理端点交付后可清理重造
