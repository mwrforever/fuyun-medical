# 任务交接文档 (Session Handoff)

> ⚠ **交接声明（务必先读）**：本文档由 `session-handoff` 技能生成，是新会话续接任务的**唯一事实来源**，内容仅来自当前会话的事实，自包含、不引用其他进度文档或历史会话记录。
> 「未完成」章节中的全部任务必须在【新会话】执行；生成本文档的会话已交接封存，不再实现任何任务。
> 新会话续接前必须完整读取本文档、以本文档为准，禁止凭空推测进度或重复已完成的工作。
> 代码与文件变更明细不在本文档记录，新会话用 `git diff` / `git log` 自行查看。

## 元信息 (Meta)
- workspace_dir: D:\code\project\fuyun-medical
- saved_at: 2026-09-18T15:35:26+08:00
- 生成方式: update
- 版本控制提交号: feat/p1-pr3-m13-billing @ 0cabcbb（Task 18 完成点；Task 19 未提交变更留在工作区，见 §3.1）
- 交接状态: 已交接（本会话封存，未完成部分须在新会话执行）
- 任务一句话描述: PR-3（M13 收费物价与医保基线，CF-4 冻结载体）SDD 执行——Task 1–18 已完成且逐任务双结论审查通过（4 个修复轮全收敛），断点=Task 19 前端三页（子代理进行至 Step 5/9 因 token 配额耗尽中断，全部产物未提交）；续接路径 Task 19 → Task 20 收口 → 终审 → PR。

## 1. 会话 Todo 追踪 (Session Todo)
- [ ] Task 19：前端收费员工作站三页（中断于 Step 5/9，产物未提交，见 §3.1）
- [ ] Task 20：收口（W-13 勘误 / TASK.md 回填与登记 / Spec 注记 / 全量门禁 / PR / `/code-review`）
- [ ] 终审：全分支 code-review + 真栈 + 浏览器真机 → PR 质量门（含 Task 13 两项 parked 资金缺口呈报用户裁决，见 §3.3）

## 2. 已完成 (Completed) — 只做精简标记
- [x] SDD 执行 Task 1–6（前置项全清）: 号段登记+门禁适配、minio 宿主端口 9003/9004、D-13 一卡通条件更新收口、D-15 与 patient 终审 minors、common 模板基类+IT 夹具收敛（FuyunStackITBase 只收敛密钥/助手/常量，容器各 IT 独占）、D-18 int64→string 根治——全部已提交。
- [x] SDD 执行 Task 7–16（billing 主体）: 迁移 V600–V605（12 表+8 事件种子）、契约层（BILL-1001~1028/六事件载荷/22 枚举/12 entity+mapper）、管理面服务与 MoneyUtil、价格版本化+快照+调价广播、计价引擎（billing_key 含患者维度）、预结算/结算（就诊卡台账 PAY）、退费分级审批、押金与一日清单、医保模拟通道（Task 12 医保分支回填闭合，BILL-1006 贯标守卫生效）、消息装配+事件消费（BILL-1013 守卫落 generateFromSource 头部）。
- [x] SDD 执行 Task 17–18（装配与验收）: fuyun-app 装配入图（BillingConfig @Import、yml 键、真栈核实种子 24/q.billing.* 两队列/28 条 billing 路径/Modulith 纳图）、双验收锚点 IT 真栈全绿（BillingSettlementFlowIT 6/6、BillingPriceSnapshotIT 3/3，HTTP+AMQP+DB 零 mock 驱动两锚点）。
- [x] 任务间审查机制: 每任务 spec+quality 双结论审查（全程 subagent）；4 个修复轮全收敛（Task 5 @Slf4j 死代码、Task 9 wrapper SQL 守卫断言、Task 14 押金余额脏写 Critical、Task 15 ecToken 日志脱敏）。
- [x] 计划审批门闭合: 用户已批准计划执行 + D-14/D-16/D-17 按默认建议裁决（前序登记，Task 20 Step 2 落点，见 §3.2）。

## 3. 未完成 (Pending) — 精简且可执行（全部须在新会话执行）

### 3.1 Task 19：前端收费员工作站三页（断点续接）
- 目标与当前状态: 交付收费员工作站三页（划价收费/退费审批/一日清单）+ money 工具 + api/billing.ts + 路由/菜单权限语义，五连门禁全绿后提交；子代理已完成 Step 1（含后端契约修复 QuoteVO.Line→QuoteLine 改名，springdoc 简单名冲突致响应行丢 unitPrice/amount/selfExpenseOnly，后端 3 文件已改且 FeeControllerTest 重跑通过，镜像已重建+容器 force-recreate，api.d.ts 已重生成：28 billing paths、int64=0）、Step 2–4（money.spec→money.ts 4 用例 PASS、api/billing.ts 全量落码）、Step 5（三页+specs 已落，DailyListView.spec.ts 撰写中）；**全部产物未提交**，grep 级状态以 `git status` / `git diff` 为准。
- 已锁定决策/阻塞风险: QuoteLine 改名遵循 PaymentLine/RefundLine 命名惯例（冻结口径）；api.d.ts 生成物入库且为前后端契约唯一来源；前端金额 string 承载零运算、`utils/money.ts` 仅做显示格式化；PageResult 用 shared 手写壳仅依赖 content/total（页/壳 page/size string 口径分歧待登记报告）；子代理已因配额中断消亡，不得假设其会继续产出。
- **下一步具体动作**: 新会话先 `git status` + `git diff` 核对 §3.1 目标清点（三页+配套 spec、billing.ts、money.ts/money.spec.ts、router/index.ts 与 AppSidebar.vue、后端 QuoteLine 三文件），补齐 DailyListView.spec.ts 及遗漏步骤，然后 `cd web && pnpm lint && pnpm format:check && pnpm type-check && pnpm test && pnpm build` 五连全绿 → 浏览器真机三页（截图落 `.superpowers/gui-test-screenshots/`）→ 分提交（建议后端 QuoteLine 修复与前端各自 conventional commit）。
- 验证方式: 五连五命令全绿 + api.d.ts 重生成 diff 为空（新鲜度，本地核对）+ 浏览器真机三页可操作、金额显示正确、无控制台报错；提交后 `git log` 可见 Task 19 提交（当前 9962cd2..HEAD 为 22 提交，Task 19 提交后 >22）。

### 3.2 Task 20：收口
- 目标与当前状态: 未开始；落点=实施计划 `docs/superpowers/plans/2026-09-17-p1-pr3-m13-billing.md` 的 Task 20（计划即权威，含全部 Steps 与命令块）。
- 已锁定决策/阻塞风险: **TASK.md 须新增登记待决策项**：押金预警阈值分叉（`BillingProperties` 代码缺省 10000 分 vs `application.yml` 500000 分、运行期 yml 生效——Task 16/17 已裁定保持现状并移交收口登记）；D-14/D-16/D-17 三项用户裁决按计划 Task 20 Step 2 落地（D-14=保留人工路径+M02 Spec §7 补办理说明一句；D-16=保留角色豁免单门禁+M02 Spec §7 unmask 注记「三态硬门禁归 M03/PR-5」；D-17=M02 Spec FU-M02-01/02 改写「证件冲突直接拒建（PAT-1002）」删 SUSPECT 强冲突承诺）；W-13=PR-2 计划 Task 10 Files 清单误列 CardUnbindRequest 的勘误，TASK.md 对应行回填删除；先记再改（CHANGELOG 收口登记）。
- **下一步具体动作**: 读计划 Task 20 全文，按其 Steps 顺序逐条执行（每步先实测取实况再落笔，禁用记忆代替命令输出）。
- 验证方式: 计划 Task 20 内给定命令全绿 + 后端全量 `JAVA_HOME=/d/code/java/jdk/jdk17 mvn -B -ntp -f backend/pom.xml verify`（24 模块）+ 前端五连 + 真栈探针（V600–V605 按序、q.billing.* 队列、api.d.ts 新鲜度，计划 Task 20 给全）。

### 3.3 终审与 PR 质量门
- 目标与当前状态: 未开始；Task 20 完成后按 SDD 终审派发全分支 code-review（MERGE_BASE=9962cd2 → HEAD），并联真栈 + 浏览器真机，然后建 PR → `/code-review` 插件审核 findings 清零 → 合并。
- 已锁定决策/阻塞风险: **必呈报用户裁决的 parked 项（未裁决前不得合并）**：Task 13 两项计划级资金缺口——①同费用并发 apply 的 TOCTOU 竞态可致重复退款；②execute 对同卡多行 CARD_BALANCE 逐行全额入账可致重复入账（写入侧允许同卡多行已实证）；候选修法（结算咨询锁/按 fee 锁/按行比例分摊）与完整证据留存于 SDD 执行台账。另：Task 13 `refund_type` 口径（医保 payer→SETTLED_REFUND、自费按计费日日窗）为授权解读，待计划层批准。
- **下一步具体动作**: 派发 SDD 终审（`review-package 计划文件 9962cd2 HEAD` + code-reviewer 提示词，指向台账 deferred-minor/parked 行供 triage）；PR 描述载 parked 裁决呈报清单与 `/code-review` 结论。
- 验证方式: 终审 findings 清零 + 六 checks 全绿 + 浏览器真机截图存档 + `/code-review` findings 清零。

## 4. 环境与全局约定 (Environment & Conventions)
- 运行时/工具版本: JDK17（Maven 命令一律前缀 `JAVA_HOME=/d/code/java/jdk/jdk17`）；Docker；pnpm；gh CLI；**token 配额 2026-09-18 耗尽、09-24 08:05 UTC 重置——新会话开工前确认配额（本会话即因配额封存）**。
- 关键配置: `deploy/.env` 不入库在位；minio 宿主 9003/9004（Task 2 已收口）；真栈当前在跑（后端镜像已被 Task 19 Step 1 重建 + 容器 force-recreate，V600–V605 已应用、event_registry=24）；前端浏览器真机产物目录 `.superpowers/gui-test-screenshots/`。
- 构建/启动命令: 见实施计划 Global Constraints 与 Task 17/19/20 内命令块（计划即权威，不另抄）；后端镜像重建 `docker build -f backend/Dockerfile -t fuyun/backend:dev backend`。
- 全局约定: 主控纯编排（实现/审查/修复一律派 subagent，串行；配额耗尽即封存会话——本会话先例）；SDD 执行台账=`<repo>/.superpowers/sdd/2026-09-17-p1-pr3-m13-billing/ledger.md`（首行计划标识，`Task N: complete` 行即已完成勿重派，deferred-minor/parked 行供终审 triage；续接从 `task-19-brief.md` 起重新生成简报派发）；conventional commits 中文 subject、body 每行 ≤100 字符（python len 自查）；先记再改；gh GraphQL EOF 改 REST `gh api repos/.../pulls -X POST -F body=@file`；推送偶发 SSH connection reset 属网络抖动重试即通；实现 PR 质量门=全量门禁 + 真栈 + 浏览器真机 → `/code-review` 插件审核通过方可合并（docs-only 免审条款不适用）。
