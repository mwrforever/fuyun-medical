# P0 出口 DoD 预检报告（PR-5 时点）

| 文档属性 | 内容 |
| --- | --- |
| 文档编号 | DOD-PRECHECK-P0-01 |
| 日期 | 2026-09-11 |
| 性质 | PR-5 B5.2 交付的 P0 出口**预检**：对交付 loop §5 七条 DoD 逐条核对当前状态、验证命令与证据来源。预检不等于终验勾选——逐项附证据的终验归 P6（loop §3-P6），本报告不提前打勾，仅记三态预检结论 |
| 状态口径 | 已满足（本时点证据可独立复核）/ 待 P6 终验附证据（动作属 P6）/ 延后条款豁免（计划明文的延后分支已按登记要求满足） |
| 预检基线 | 分支 `feat/pr5-p0-closeout`（B5.1 已交付：bigscreen 遥测页 + workstation 首页骨架 + 50 例单测）；PR-1~PR-4 已合入 dev（#4/ed5e34e、#5/a019f47、#6/a93179a、#7/9107f92） |
| 依据 | `docs/prompt/2026-09-09-loop-P0工程骨架.md` §5 七条（第 119-125 行）；`docs/plans/2026-09-08-P0实施计划.md` §3 DoD |
| 台账 | `.superpowers/sdd/ledger.md`（阶段状态总表 + 门禁证据（P0）节 + 批次明细） |

## 预检结论总表

| # | DoD 条目（loop §5） | 预检状态 | 证据来源 / 验证命令 | 备注 |
| --- | --- | --- | --- | --- |
| 1 | compose 七服务全 healthy；`--profile sim` 端到端演示通过（IoTDA→AMQP→TimescaleDB→WebSocket），或 IOTDA_* 缺失按计划延后登记 | 延后条款豁免（登记分支已满足；healthy 复跑与演示归 P6） | 延后登记证据 = TASK.md 延后事项表 L-1~L-4 行（IOTDA_* 六变量用户侧缺失）；backend 侧启用接线已闭环（`FUYUN_IOT_AMQP_ENABLED`/`FUYUN_IOT_AMQP_QUEUES` env 透传 + 空队列名 fail-fast，L-1 行载明启用前提） | P6 复跑：`docker compose -f deploy/docker-compose.yml --env-file deploy/.env up -d` 后 `docker compose ps` 验七服务全 healthy；六变量就绪后补 `--profile sim` 全链路演示记录并回填删除 L-1~L-4 |
| 2 | 后端门禁绿含 JaCoCo 双阈值（核心包 1.00）；前端五门禁绿（本地输出+CI 日志） | 已满足 | 后端 = PR #7 CI `backend / verify` 绿（342 单测 + 38 IT；JaCoCo integration/system/iot 三核心包 PACKAGE LINE=1.00）；PR-5 后端零改动免复跑。前端 = B5.1 六门禁本地全绿 50 例（web 宪法 C.5-1 六步，loop「五门禁」为概称——台账冲突扫描第 2 条已裁决按宪法六步执行）；随本 PR CI frontend job 复证 | 本地复跑命令（已实测可执行）：`cd backend && JAVA_HOME=/d/code/java/jdk/jdk17 mvn -B -ntp verify`；`cd web && pnpm lint && pnpm format:check && pnpm type-check && pnpm test && pnpm build && pnpm audit --audit-level high` |
| 3 | 五个 PR 均经 /code-review 审核通过后合入 dev | 待 P6 终验附证据 | 已过 = PR #4/#6/#7（台账 P1/P3/P4 行含 findings 修复与复审记录）；缺口 = PR #5 PR 时点未单独派遣 /code-review（台账 P2 行 + 交接文档 §2.3 登记，P6 对 diff `ed5e34e..a019f47` 补跑）；PR-5 本体随本 PR 走 /code-review | P6 汇总五个 PR 的 /code-review 记录落终验报告 |
| 4 | main 与 dev 保护均含五 required checks（gh api 输出落终验报告） | 待 P6 终验附证据（P0 证据已在位） | P0 证据 = 台账「门禁证据（P0）」节 gh api 输出：dev 保护与 main 逐字段一致（contexts = backend / verify、frontend / verify、images、commitlint、hygiene；strict=true、enforce_admins=true、禁 force push/删除） | P6 以 `gh api repos/{owner}/{repo}/branches/main/protection` 与 `.../branches/dev/protection` 复证，输出原文落终验报告 |
| 5 | CF-1/CF-2/CF-7 冻结登记 event_registry（迁移+测试断言） | 已满足 | 迁移 = `backend/fuyun-integration/.../V5__seed_event_registry_cf2_cf7.sql`（CF-1 信封约定 + CF-2 五主数据事件 + CF-7 遥测模型）与 `backend/fuyun-iot/.../V403__seed_iot_event_registry.sql`；断言 = `MessagingGovernanceIT` 冻结断言（PR-2/PR-4 交付，随 #5/#7 CI 绿） | 无动作；真实 IoTDA 报文映射冻结另见 TASK.md L-3（联调时点） |
| 6 | TASK.md：W-3 销项、T-R3-2/T-R3-3 回填实测结论、新发现待决策项登记 | 已满足（B5.2 本批闭环） | W-3 = 三项对齐逐项核实后删除整行（① Dockerfile 逐模块显式 COPY；② web 产物路径 compose/Dockerfile/nginx 三处同路径；③ ci.yml 无骨架期排除）；T-R3-2 = PR-4 B4.1 实测回填删除（`add_columnstore_policy` 胜出，CHANGELOG 2026-09-10 B4.1 条目）；T-R3-3 = B4.4 本地两级实测回填，结论并入 TASK.md L-2 行；新发现待决策项已按条登记（D-6/D-7/D-8 等） | B5.2 核对声明：TASK.md 待调研表确无 T-R3-2/T-R3-3 行，L-2 行在位且表述完整，无文件改动 |
| 7 | 台账完整；终验报告每项附证据并合入 dev；本地最终停留 dev | 待 P6 终验 | 台账机制在位 = `.superpowers/sdd/ledger.md` 阶段状态总表（P0~P4 complete、P5 in_progress）与批次明细逐批回填至 B5.1 | 终验报告（`docs/prompt/2026-09-09-P0终验报告.md`）撰写与合入 dev、台账收尾、确认本地停留 dev 均属 P6 动作 |

## 预检声明

1. 本报告为 PR-5 时点预检：第 1/3/4/7 条的「待 P6 / 延后豁免」状态不构成 P5 出口门禁失败——按 loop §3-P5，PR-5 出口门禁为「计划 §1-PR-5 全过 + DoD 预检 + 合入 dev」，预检即本报告本身。
2. 第 2/5/6 条「已满足」项的证据均可在本时点独立复核（CI 运行记录、迁移与 IT 文件、TASK.md 与台账现状）；第 1 条延后登记分支的满足口径来自计划与 loop 原文「或 IOTDA_* 缺失按计划延后登记」。
3. 预检过程未发现新的待登记事项；PR-5 行完成标注与 §3 DoD 勾选留待 P6 终验随证据一并补记。
